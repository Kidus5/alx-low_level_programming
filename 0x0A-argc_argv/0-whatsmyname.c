#include <stdio.h>

/**
 * main - prints its name
 * @argv: the array holding the command line input
 * @argc: the number of commands put in
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
